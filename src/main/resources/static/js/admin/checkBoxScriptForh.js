function updateValue1() {
    const checkbox = document.getElementById('checkbox84');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin1?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })
}

function updateValue2() {
    const checkbox = document.getElementById('checkbox85');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin2?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue3() {
    const checkbox = document.getElementById('checkbox86');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin3?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue4() {
    const checkbox = document.getElementById('checkbox87');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin4?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue5() {
    const checkbox = document.getElementById('checkbox88');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin5?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue6() {
    const checkbox = document.getElementById('checkbox89');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin6?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue7() {
    const checkbox = document.getElementById('checkbox90');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin7?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue8() {
    const checkbox = document.getElementById('checkbox91');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin8?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue9() {
    const checkbox = document.getElementById('checkbox92');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin9?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue10() {
    const checkbox = document.getElementById('checkbox93');
    const id = 6; // Идентификатор записи, которую нужно обновить

    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin10?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue11() {
    const checkbox = document.getElementById('checkbox94');
    const id = 6; // Идентификатор записи, которую нужно обновить

    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin11?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue12() {
    const checkbox = document.getElementById('checkbox95');
    const id = 6; // Идентификатор записи, которую нужно обновить

    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin12?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue13() {
    const id = 6; // Идентификатор записи, которую нужно обновить

    const checkbox = document.getElementById('checkbox96');
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin13?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue14() {
    const id = 6; // Идентификатор записи, которую нужно обновить

    const checkbox = document.getElementById('checkbox97');
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin14?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue15() {
    const id = 6; // Идентификатор записи, которую нужно обновить

    const checkbox = document.getElementById('checkbox98');
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin15?id=' + id + '&isChecked=' + isChecked, {

        method: 'POST'
    })

}

function updateValue16() {
    const checkbox = document.getElementById('checkbox99');
    const id = 6; // Идентификатор записи, которую нужно обновить

    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin16?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })

}

function updateValue17() {
    const id = 6; // Идентификатор записи, которую нужно обновить

    const checkbox = document.getElementById('checkbox100');
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin17?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })

}

function updateValue18() {
    const id = 6; // Идентификатор записи, которую нужно обновить

    const checkbox = document.getElementById('checkbox101');
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin18?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })

}

function updateValue19() {
    const id = 6; // Идентификатор записи, которую нужно обновить

    const checkbox = document.getElementById('checkbox102');
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin19?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })

}

function updateValue20() {
    const checkbox = document.getElementById('checkbox103');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin20?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}

function updateValue21() {
    const checkbox = document.getElementById('checkbox104');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin21?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}


function updateValue22() {
    const checkbox = document.getElementById('checkbox105');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin22?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}

function updateValue23() {
    const checkbox = document.getElementById('checkbox106');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin23?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}

function updateValue24() {
    const checkbox = document.getElementById('checkbox107');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin24?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}

function updateValue25() {
    const checkbox = document.getElementById('checkbox108');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin25?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}

function updateValue26() {
    const checkbox = document.getElementById('checkbox109');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin26?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}
function updateValue27() {
    const checkbox = document.getElementById('checkbox110');
    const id = 6; // Идентификатор записи, которую нужно обновить
    const isChecked = checkbox.checked; // Значение чекбокса (true или false)

    fetch('/admin/admin27?id=' + id + '&isChecked=' + isChecked, {
        method: 'POST'
    })
}